services:
  ccfire:
    build: .
    container_name: ccfire
    restart: unless-stopped
    ports:
      - "8283:8283"
      - "8322:22"
    volumes:
      - ./outputs:/app/outputs
      - ccfire-claude-config:/home/ccfire/.claude

volumes:
  ccfire-claude-config:
