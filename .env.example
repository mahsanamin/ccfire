# ─── Mode ───
# "local" = run directly on host (full toolchain access)
# "docker" = run in container (lightweight, isolated)
CCFIRE_MODE=local

# ─── Projects directory ───
# Local mode: absolute path to your repos
# Docker mode: gets mounted at /projects inside the container
PROJECTS_DIR=~/repos
